#include <stdio.h>
#include <math.h>
int main()
{
    int n, p;

    printf("Enter number : ");
    scanf("%d", &n);

    printf("Pow number : ");
    scanf("%d", &p);

    // printf("\n%d^%d = %d", n, p, (int)pow(n, p));
    return 0;
}